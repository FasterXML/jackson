/**
 *Contains public annotations, most of which are used
 *to configure how Data Binding works.
 *It also contains parameter types (mostly enums) needed by annotations, and
 *also a dummy marker class {@link org.codehaus.jackson.annotate.NoClass},
 *which is needed to
 *work around the problem of 'null' not being valid value for
 *annotation properties.
 */

package org.codehaus.jackson.annotate;
